<%= render 'add-new-form' %>

<% if @films.any? %>
    <ul>
        <% @films.each do |film| %>
            <li>
            <%= render 'film', film: film %>
        <% end %>
    </ul>
<% else %>
    <p>No films to show</p>
<% end %>